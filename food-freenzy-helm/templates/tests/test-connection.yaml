apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "food-freenzy-helm.fullname" . }}-test-connection"
  labels:
    {{- include "food-freenzy-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "food-freenzy-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
