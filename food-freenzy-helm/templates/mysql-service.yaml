apiVersion: v1
kind: Service
metadata:
  name: {{ include "food-freenzy-helm.mysql.name" . }}
  labels:
    {{- include "food-freenzy-helm.mysql.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
  - port: {{ .Values.mysql.port }}
    targetPort: 3306
    protocol: TCP
  selector:
    {{- include "food-freenzy-heelm.mysql.selectorLabels" . | nindent 4 }}
